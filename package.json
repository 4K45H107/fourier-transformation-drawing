{
    "name": "fourier-transformation-drawing",
    "version": "1.0.0",
    "scripts": {
      "dev": "browser-sync start --server --files '*.html, js/**/*.js, css/**/*.css' --no-notify --open"
    },
    "devDependencies": {
      "browser-sync": "^2.29.3"
    }
  }